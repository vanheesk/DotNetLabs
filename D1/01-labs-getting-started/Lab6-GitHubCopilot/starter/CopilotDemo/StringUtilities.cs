namespace CopilotDemo;

/// <summary>
/// A collection of string utility methods.
/// Use GitHub Copilot to implement each method based on the TODO comments.
/// </summary>
public static class StringUtilities
{
    // TODO: Reverse the input string and return the result.
    // Example: "Hello" → "olleH"
    public static string Reverse(string input)
    {
        throw new NotImplementedException();
    }

    // TODO: Check if the input string is a palindrome (reads the same forwards and backwards).
    // Should be case-insensitive and ignore whitespace.
    // Examples: "racecar" → true, "Race Car" → true, "hello" → false
    public static bool IsPalindrome(string input)
    {
        throw new NotImplementedException();
    }

    // TODO: Count the number of words in the input string.
    // Words are separated by whitespace. Multiple spaces should count as a single separator.
    // An empty or null string should return 0.
    public static int WordCount(string? input)
    {
        throw new NotImplementedException();
    }

    // TODO: Truncate the string to the specified maximum length.
    // If the string is longer than maxLength, cut it and append "..." (the total length
    // including "..." should not exceed maxLength + 3).
    // If the string is shorter or equal, return it unchanged.
    public static string Truncate(string input, int maxLength)
    {
        throw new NotImplementedException();
    }

    // TODO: Convert the input string to Title Case (capitalize the first letter of each word).
    // Example: "hello world" → "Hello World"
    public static string ToTitleCase(string input)
    {
        throw new NotImplementedException();
    }
}
